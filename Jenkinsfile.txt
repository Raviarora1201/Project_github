pipeline {
 agent any
    stages {
        stage('init') {
           steps{ sh 'sudo /terraform/test2/terraform init'}
		     }
		
        stage('plan') {
            steps{ sh 'sudo /terraform/test2/terraform plan'}
                     }


             }
}
