pipeline {
 agent any
    stages {
        stage('init') {
           sh 'sudo /terraform/test2/terraform init'
		     }
		
        stage('plan') {
            sh 'sudo /terraform/test2/terraform plan'
                     }


             }
}
